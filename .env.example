# Copy this file to `.env.local` and fill in the keys you need.
# .env.local is gitignored.

# For `npm run gen-scenes` (OpenAI gpt-image-1)
OPENAI_API_KEY=sk-...

# For `npm run gen-scenes-nano` (Google Gemini 2.5 Flash Image, a.k.a. Nano Banana)
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
